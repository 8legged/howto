<h3>All Tasks</h3>
add task [<%= link_to "+", new_task_path %>]<br /><br />


<% @tasks.each do |task| %>
  <ul>
    <li>
      <%= link_to task.title, task %> (<%= task.steps.count %>)
      <%= link_to "Delete", task, action: "delete", method: :delete, data: { confirm: "Are you sure?" } %>
    </li>
  </ul>
  <% end %>
